name: dev
mode: real
storage_dir: artifacts/dev
db_path: artifacts/dev/metadata.db
default_backend: openai
backend_configs:
  openai:
    provider: openai-compatible
    base_url: https://api.openai.com/v1
    api_key_env: OPENAI_API_KEY

